on:
  push: 
    branches:
      - main

jobs:
  auto_correct:
    name: AutoCorrect
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: AutoCorrect
        uses: huacnlee/autocorrect-action@v2
